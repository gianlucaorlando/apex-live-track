# Optional. Historical OpenF1 data usually works without a token.
# Live data may require a paid OpenF1 subscription.
OPENF1_API_TOKEN=

# Optional. Enables the Google Maps Static background layer.
# Requires Maps Static API enabled in Google Cloud.
GOOGLE_MAPS_API_KEY=
