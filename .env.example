# Optional. Historical OpenF1 data usually works without a token.
# Live data may require a paid OpenF1 subscription.
OPENF1_API_TOKEN=

# Optional alternative for live streaming. If set, the server refreshes the
# OpenF1 access token automatically for REST and MQTT-over-WebSocket.
OPENF1_USERNAME=
OPENF1_PASSWORD=
